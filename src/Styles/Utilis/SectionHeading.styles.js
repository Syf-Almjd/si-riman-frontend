import { Padding } from "@mui/icons-material";

const styles = {
    Heading: {
        width: "max-content",
        fontWeight: 700,
        letterSpacing: "1px",
        fontSize: {
            sm: "1.5rem",
            xxs: "1.2rem"
        }
    },
    Divider: {
        mx: "24%",
        borderWidth: "2px",
        borderColor: "primary.main",
        mt: "20px"
    }
};

export default styles;